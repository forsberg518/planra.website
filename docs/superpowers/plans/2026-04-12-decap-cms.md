# Decap CMS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Set up Decap CMS so planra.no editors can change texts, prices, and images at planra.no/admin without touching code.

**Architecture:** Content is extracted from index.html into `content.json`. A script `cms-inject.js` fetches that file at page load and injects values via `data-cms` attributes. Decap CMS edits `content.json` and images via GitHub OAuth, with two small Vercel serverless functions handling the OAuth flow.

**Tech Stack:** Decap CMS (CDN), GitHub backend, Vercel serverless functions (Node.js), vanilla JS for injection.

---

## File Map

| File | Action | Responsibility |
|------|--------|---------------|
| `content.json` | Create | Single source of truth for all editable content |
| `cms-inject.js` | Create | Fetches content.json, injects values into DOM |
| `index.html` | Modify | Add `data-cms` attributes + load cms-inject.js |
| `admin/index.html` | Create | Decap CMS admin panel (loads CMS from CDN) |
| `admin/config.yml` | Create | CMS field definitions and GitHub backend config |
| `api/auth.js` | Create | Starts GitHub OAuth flow |
| `api/callback.js` | Create | Completes OAuth, sends token to CMS |

---

## Task 1: Create content.json

**Files:**
- Create: `content.json`

- [ ] **Step 1: Create content.json with all current values**

```json
{
  "hero_badge": "Nå tilgjengelig på App Store",
  "hero_tittel_1": "Din komplette",
  "hero_tittel_highlight": "PT-app",
  "hero_tittel_3": "for trenere og kunder",
  "hero_tekst": "Planra samler timeplan, kunder, fakturering og chat i én enkel app. Perfekt for norske personlige trenere som vil drive virksomheten sin smartere.",

  "trener_overskrift": "Alt du trenger for å drive",
  "trener_overskrift_highlight": "en suksessfull PT-virksomhet",
  "trener_tekst": "Planra gir deg full oversikt over alle kunder, timer og inntekter. Bruk mer tid på det du er god på – å trene folk.",
  "trener_bilde": "/section-hjem-trener.png",
  "trener_funksjoner": [
    { "tittel": "Smart kalender", "beskrivelse": "Uke- og månedsoversikt med alle timene dine. Se raskt hvem som skal trene og når." },
    { "tittel": "Kundeadministrasjon", "beskrivelse": "Hold oversikt over alle kunder, timhistorikk, mål og fremgang – alt samlet ett sted." },
    { "tittel": "Fakturering og betaling", "beskrivelse": "Spor inntekter, marker timer som fakturert eller betalt. Enkel oversikt over hva du tjener." },
    { "tittel": "Push-varsler", "beskrivelse": "Automatiske påminnelser til kunder 1 time før økt, og varsler om nye meldinger." }
  ],

  "kunde_overskrift": "Hold deg motivert",
  "kunde_overskrift_highlight": "og nå målene dine",
  "kunde_tekst": "Som kunde har du alltid full oversikt over neste økt, fremgang mot mål og direkte kontakt med treneren din.",
  "kunde_bilde": "/section-hjem-kunde.png",
  "kunde_funksjoner": [
    { "tittel": "Be om time", "beskrivelse": "Send timerequest til treneren din med ønsket treningstype og notater. Enkelt og raskt." },
    { "tittel": "Følg fremgang", "beskrivelse": "Se visualisert fremgang mot alle målene dine. Logg egne treninger og se statistikk." },
    { "tittel": "Chat med treneren", "beskrivelse": "Hold kontakten mellom øktene. Still spørsmål, del oppdateringer og få motivasjon." },
    { "tittel": "Nedtelling til neste økt", "beskrivelse": "Alltid en visuell nedtelling til neste time, så du aldri mister motivasjonen." }
  ],

  "chat_overskrift": "Hold kontakten mellom",
  "chat_overskrift_highlight": "øktene",
  "chat_tekst": "Med sanntids chat i Planra trenger du ikke WhatsApp, SMS eller e-post for å kommunisere med kundene dine. Alt skjer direkte i appen – alltid organisert og oversiktlig.",
  "chat_bilde": "/section-chat.png",

  "pris_trener_kr": "249",
  "pris_proveperiode": "30 dager gratis prøveperiode",
  "pris_trener_punkter": [
    "Ubegrenset antall kunder",
    "Timeplan og booking",
    "Fakturering og betaling",
    "Sanntids chat",
    "Integrasjoner (Fiken, Google Kalender)"
  ],
  "pris_kunde_punkter": [
    "Book timer med din PT",
    "Chat direkte med treneren din",
    "Se din treningsplan og fremgang",
    "Betaling til treneren din",
    "Push-varsler og påminnelser"
  ],

  "cta_overskrift_1": "Klar til å ta",
  "cta_overskrift_highlight": "PT-hverdagen din",
  "cta_overskrift_3": "til neste nivå?",
  "cta_tekst": "Start din 30 dager gratis prøveperiode og opplev den enkleste måten å administrere PT-virksomheten din på.",

  "faq": [
    {
      "sporsmal": "Koster Planra noe?",
      "svar": "Planra koster 249 kr/mnd for trenere, med 30 dagers gratis prøveperiode – ingen kredittkort kreves for å starte. For kunder av personlige trenere er Planra alltid helt gratis."
    },
    {
      "sporsmal": "Hvordan inviterer jeg kundene mine som trener?",
      "svar": "Gå til «Kunder»-fanen og trykk inviter. Du får en lenke du kan sende via SMS, e-post eller annen kanal. Kunden laster ned appen, klikker lenken og er koblet til deg på sekunder."
    },
    {
      "sporsmal": "Hvilke plattformer støtter Planra?",
      "svar": "Planra er nå tilgjengelig på iOS (iPhone og iPad) via App Store. Android-versjon via Google Play er under utvikling og kommer snart."
    },
    {
      "sporsmal": "Er dataene mine trygge?",
      "svar": "Ja. All data krypteres i overføring og i ro. Vi følger GDPR og selger aldri data til tredjeparter. Les mer i vår personvernerklæring."
    },
    {
      "sporsmal": "Fungerer Planra uten internett?",
      "svar": "Appen krever internett for å synkronisere og sende meldinger. Allerede innlastet informasjon vises midlertidig uten nett, men endringer lagres ikke uten tilkobling."
    }
  ]
}
```

- [ ] **Step 2: Commit**

```bash
git add content.json
git commit -m "feat: add content.json for CMS-managed content"
```

---

## Task 2: Create cms-inject.js

**Files:**
- Create: `cms-inject.js`

- [ ] **Step 1: Create cms-inject.js**

```javascript
(async () => {
  function esc(str) {
    return String(str ?? '')
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;');
  }

  function set(sel, val) {
    const el = document.querySelector(sel);
    if (el && val !== undefined) el.textContent = val;
  }

  function setSrc(sel, val) {
    const el = document.querySelector(sel);
    if (el && val !== undefined) el.src = val;
  }

  function attachFaqListeners() {
    document.querySelectorAll('.faq-trigger-index').forEach(trigger => {
      trigger.addEventListener('click', () => {
        const item = trigger.closest('.faq-item-index');
        const answer = item.querySelector('.faq-answer-index');
        const icon = trigger.querySelector('.faq-icon-index');
        const isOpen = answer.style.gridTemplateRows === '1fr';
        document.querySelectorAll('.faq-item-index').forEach(el => {
          el.querySelector('.faq-answer-index').style.gridTemplateRows = '0fr';
          el.querySelector('.faq-icon-index').style.transform = 'rotate(0deg)';
        });
        if (!isOpen) {
          answer.style.gridTemplateRows = '1fr';
          icon.style.transform = 'rotate(45deg)';
        }
      });
    });
  }

  try {
    const res = await fetch('/content.json?v=' + Date.now());
    if (!res.ok) return;
    const c = await res.json();

    // Hero
    set('[data-cms="hero_badge"]', c.hero_badge);
    set('[data-cms="hero_tittel_1"]', c.hero_tittel_1);
    set('[data-cms="hero_tittel_highlight"]', c.hero_tittel_highlight);
    set('[data-cms="hero_tittel_3"]', c.hero_tittel_3);
    set('[data-cms="hero_tekst"]', c.hero_tekst);

    // Trener
    set('[data-cms="trener_overskrift"]', c.trener_overskrift);
    set('[data-cms="trener_overskrift_highlight"]', c.trener_overskrift_highlight);
    set('[data-cms="trener_tekst"]', c.trener_tekst);
    setSrc('[data-cms-src="trener_bilde"]', c.trener_bilde);
    (c.trener_funksjoner || []).forEach((f, i) => {
      set(`[data-cms="trener_f${i + 1}_tittel"]`, f.tittel);
      set(`[data-cms="trener_f${i + 1}_tekst"]`, f.beskrivelse);
    });

    // Kunde
    set('[data-cms="kunde_overskrift"]', c.kunde_overskrift);
    set('[data-cms="kunde_overskrift_highlight"]', c.kunde_overskrift_highlight);
    set('[data-cms="kunde_tekst"]', c.kunde_tekst);
    setSrc('[data-cms-src="kunde_bilde"]', c.kunde_bilde);
    (c.kunde_funksjoner || []).forEach((f, i) => {
      set(`[data-cms="kunde_f${i + 1}_tittel"]`, f.tittel);
      set(`[data-cms="kunde_f${i + 1}_tekst"]`, f.beskrivelse);
    });

    // Chat
    set('[data-cms="chat_overskrift"]', c.chat_overskrift);
    set('[data-cms="chat_overskrift_highlight"]', c.chat_overskrift_highlight);
    set('[data-cms="chat_tekst"]', c.chat_tekst);
    setSrc('[data-cms-src="chat_bilde"]', c.chat_bilde);

    // Pris
    set('[data-cms="pris_trener_kr"]', c.pris_trener_kr);
    set('[data-cms="pris_proveperiode"]', c.pris_proveperiode);
    (c.pris_trener_punkter || []).forEach((p, i) => {
      set(`[data-cms="pris_trener_p${i + 1}"]`, p);
    });
    (c.pris_kunde_punkter || []).forEach((p, i) => {
      set(`[data-cms="pris_kunde_p${i + 1}"]`, p);
    });

    // CTA
    set('[data-cms="cta_overskrift_1"]', c.cta_overskrift_1);
    set('[data-cms="cta_overskrift_highlight"]', c.cta_overskrift_highlight);
    set('[data-cms="cta_overskrift_3"]', c.cta_overskrift_3);
    set('[data-cms="cta_tekst"]', c.cta_tekst);

    // FAQ — rebuild HTML then re-attach accordion listeners
    if (c.faq && c.faq.length) {
      const container = document.getElementById('faq-index');
      if (container) {
        container.innerHTML = c.faq.map(item => `
          <div class="faq-item-index glass rounded-2xl overflow-hidden reveal">
            <button class="faq-trigger-index w-full flex items-center justify-between px-5 py-4 text-left gap-4">
              <span class="font-medium text-sm sm:text-base">${esc(item.sporsmal)}</span>
              <svg class="faq-icon-index w-5 h-5 text-slate-400 flex-shrink-0 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
              </svg>
            </button>
            <div class="faq-answer-index" style="display:grid;grid-template-rows:0fr;transition:grid-template-rows 0.3s ease;">
              <div style="overflow:hidden;"><div class="px-5 pb-4 text-slate-400 text-sm leading-relaxed">${esc(item.svar)}</div></div>
            </div>
          </div>
        `).join('');
        attachFaqListeners();
      }
    }

  } catch (e) {
    console.warn('CMS inject failed:', e);
  }
})();
```

- [ ] **Step 2: Commit**

```bash
git add cms-inject.js
git commit -m "feat: add cms-inject.js to load content.json into DOM"
```

---

## Task 3: Add data-cms attributes to index.html + load script

**Files:**
- Modify: `index.html`

This is the largest task. Add `data-cms` attributes to each editable element, and load `cms-inject.js` before `</body>`.

- [ ] **Step 1: Hero — badge text (around line 811)**

Find:
```html
            Nå tilgjengelig på App Store
```
Replace with:
```html
            <span data-cms="hero_badge">Nå tilgjengelig på App Store</span>
```

- [ ] **Step 2: Hero — title lines (around line 814–819)**

Find:
```html
          <h1 class="text-4xl sm:text-5xl lg:text-6xl font-black leading-[1.08] tracking-[-0.02em] mb-6">
            Din komplette
            <br />
            <span class="gradient-text">PT-app</span>
            <br />
            for trenere og kunder
          </h1>
```
Replace with:
```html
          <h1 class="text-4xl sm:text-5xl lg:text-6xl font-black leading-[1.08] tracking-[-0.02em] mb-6">
            <span data-cms="hero_tittel_1">Din komplette</span>
            <br />
            <span class="gradient-text" data-cms="hero_tittel_highlight">PT-app</span>
            <br />
            <span data-cms="hero_tittel_3">for trenere og kunder</span>
          </h1>
```

- [ ] **Step 3: Hero — subtitle paragraph (around line 822–826)**

Find:
```html
          <p class="text-lg text-slate-400 leading-relaxed mb-8 max-w-md">
            Planra samler timeplan, kunder, fakturering og chat i én enkel app.
            Perfekt for norske personlige trenere som vil drive virksomheten sin
            smartere.
          </p>
```
Replace with:
```html
          <p class="text-lg text-slate-400 leading-relaxed mb-8 max-w-md" data-cms="hero_tekst">
            Planra samler timeplan, kunder, fakturering og chat i én enkel app.
            Perfekt for norske personlige trenere som vil drive virksomheten sin
            smartere.
          </p>
```

- [ ] **Step 4: Trener section — heading and text (around line 974–981)**

Find:
```html
            <h2 class="text-3xl sm:text-4xl font-black leading-tight tracking-[-0.02em] mb-4">
              Alt du trenger for å drive<br />
              <span class="gradient-text-blue">en suksessfull PT-virksomhet</span>
            </h2>
            <p class="text-slate-400 leading-relaxed mb-8">
              Planra gir deg full oversikt over alle kunder, timer og inntekter.
              Bruk mer tid på det du er god på – å trene folk.
            </p>
```
Replace with:
```html
            <h2 class="text-3xl sm:text-4xl font-black leading-tight tracking-[-0.02em] mb-4">
              <span data-cms="trener_overskrift">Alt du trenger for å drive</span><br />
              <span class="gradient-text-blue" data-cms="trener_overskrift_highlight">en suksessfull PT-virksomhet</span>
            </h2>
            <p class="text-slate-400 leading-relaxed mb-8" data-cms="trener_tekst">
              Planra gir deg full oversikt over alle kunder, timer og inntekter.
              Bruk mer tid på det du er god på – å trene folk.
            </p>
```

- [ ] **Step 5: Trener section — feature points (around line 988–1016)**

Find and replace each feature title and description with data-cms attributes:

```html
                  <div class="font-semibold mb-0.5" data-cms="trener_f1_tittel">Smart kalender</div>
                  <div class="text-sm text-slate-400" data-cms="trener_f1_tekst">Uke- og månedsoversikt med alle timene dine. Se raskt hvem som skal trene og når.</div>
```
```html
                  <div class="font-semibold mb-0.5" data-cms="trener_f2_tittel">Kundeadministrasjon</div>
                  <div class="text-sm text-slate-400" data-cms="trener_f2_tekst">Hold oversikt over alle kunder, timhistorikk, mål og fremgang – alt samlet ett sted.</div>
```
```html
                  <div class="font-semibold mb-0.5" data-cms="trener_f3_tittel">Fakturering og betaling</div>
                  <div class="text-sm text-slate-400" data-cms="trener_f3_tekst">Spor inntekter, marker timer som fakturert eller betalt. Enkel oversikt over hva du tjener.</div>
```
```html
                  <div class="font-semibold mb-0.5" data-cms="trener_f4_tittel">Push-varsler</div>
                  <div class="text-sm text-slate-400" data-cms="trener_f4_tekst">Automatiske påminnelser til kunder 1 time før økt, og varsler om nye meldinger.</div>
```

- [ ] **Step 6: Trener — phone image (around line 958)**

Find:
```html
              <img
                src="section-hjem-trener.png"
                alt="Planra Hjem-skjerm for trener"
                class="w-[280px]"
                style="height: auto;"
              />
```
Replace with:
```html
              <img
                src="section-hjem-trener.png"
                alt="Planra Hjem-skjerm for trener"
                class="w-[280px]"
                style="height: auto;"
                data-cms-src="trener_bilde"
              />
```

- [ ] **Step 7: Kunde section — heading and text (around line 1051–1058)**

Find:
```html
            <h2 class="text-3xl sm:text-4xl font-black leading-tight tracking-[-0.02em] mb-4">
              Hold deg motivert<br />
              <span class="gradient-text">og nå målene dine</span>
            </h2>
            <p class="text-slate-400 leading-relaxed mb-8">
              Som kunde har du alltid full oversikt over neste økt, fremgang mot
              mål og direkte kontakt med treneren din.
            </p>
```
Replace with:
```html
            <h2 class="text-3xl sm:text-4xl font-black leading-tight tracking-[-0.02em] mb-4">
              <span data-cms="kunde_overskrift">Hold deg motivert</span><br />
              <span class="gradient-text" data-cms="kunde_overskrift_highlight">og nå målene dine</span>
            </h2>
            <p class="text-slate-400 leading-relaxed mb-8" data-cms="kunde_tekst">
              Som kunde har du alltid full oversikt over neste økt, fremgang mot
              mål og direkte kontakt med treneren din.
            </p>
```

- [ ] **Step 8: Kunde — feature points (around line 1065–1094)**

```html
                  <div class="font-semibold mb-0.5" data-cms="kunde_f1_tittel">Be om time</div>
                  <div class="text-sm text-slate-400" data-cms="kunde_f1_tekst">Send timerequest til treneren din med ønsket treningstype og notater. Enkelt og raskt.</div>
```
```html
                  <div class="font-semibold mb-0.5" data-cms="kunde_f2_tittel">Følg fremgang</div>
                  <div class="text-sm text-slate-400" data-cms="kunde_f2_tekst">Se visualisert fremgang mot alle målene dine. Logg egne treninger og se statistikk.</div>
```
```html
                  <div class="font-semibold mb-0.5" data-cms="kunde_f3_tittel">Chat med treneren</div>
                  <div class="text-sm text-slate-400" data-cms="kunde_f3_tekst">Hold kontakten mellom øktene. Still spørsmål, del oppdateringer og få motivasjon.</div>
```
```html
                  <div class="font-semibold mb-0.5" data-cms="kunde_f4_tittel">Nedtelling til neste økt</div>
                  <div class="text-sm text-slate-400" data-cms="kunde_f4_tekst">Alltid en visuell nedtelling til neste time, så du aldri mister motivasjonen.</div>
```

- [ ] **Step 9: Kunde — phone image (around line 1035)**

Find:
```html
              <img
                src="section-hjem-kunde.png"
                alt="Planra Hjem-skjerm for kunde"
                class="w-[280px]"
                style="height: auto;"
              />
```
Replace with:
```html
              <img
                src="section-hjem-kunde.png"
                alt="Planra Hjem-skjerm for kunde"
                class="w-[280px]"
                style="height: auto;"
                data-cms-src="kunde_bilde"
              />
```

- [ ] **Step 10: Chat section — heading and text (around line 1236–1242)**

Find:
```html
            <h2 class="text-3xl sm:text-4xl font-black leading-tight tracking-[-0.02em] mb-4">
              Hold kontakten mellom<br />
              <span class="gradient-text-blue">øktene</span>
            </h2>
            <p class="text-slate-400 leading-relaxed mb-6">
              Med sanntids chat i Planra trenger du ikke WhatsApp, SMS eller e-post for å kommunisere med kundene dine.
              Alt skjer direkte i appen – alltid organisert og oversiktlig.
            </p>
```
Replace with:
```html
            <h2 class="text-3xl sm:text-4xl font-black leading-tight tracking-[-0.02em] mb-4">
              <span data-cms="chat_overskrift">Hold kontakten mellom</span><br />
              <span class="gradient-text-blue" data-cms="chat_overskrift_highlight">øktene</span>
            </h2>
            <p class="text-slate-400 leading-relaxed mb-6" data-cms="chat_tekst">
              Med sanntids chat i Planra trenger du ikke WhatsApp, SMS eller e-post for å kommunisere med kundene dine.
              Alt skjer direkte i appen – alltid organisert og oversiktlig.
            </p>
```

- [ ] **Step 11: Chat — phone image (around line 1267)**

Find:
```html
              <img
                src="section-chat.png"
                alt="Planra Chat-skjerm"
                class="w-[280px]"
                style="height: auto;"
              />
```
Replace with:
```html
              <img
                src="section-chat.png"
                alt="Planra Chat-skjerm"
                class="w-[280px]"
                style="height: auto;"
                data-cms-src="chat_bilde"
              />
```

- [ ] **Step 12: Pris — price number and trial text**

Find:
```html
                <span class="text-5xl font-black tracking-tight">249</span>
                  <span class="text-slate-400 text-lg mb-1.5">kr/mnd</span>
                </div>
                <p class="text-slate-400 text-sm">30 dager gratis prøveperiode</p>
```
Replace with:
```html
                <span class="text-5xl font-black tracking-tight" data-cms="pris_trener_kr">249</span>
                  <span class="text-slate-400 text-lg mb-1.5">kr/mnd</span>
                </div>
                <p class="text-slate-400 text-sm" data-cms="pris_proveperiode">30 dager gratis prøveperiode</p>
```

- [ ] **Step 13: Pris — trainer feature list items**

In the trainer pricing card, add data-cms attributes to each feature list item text span. Find each `<li>` in the trainer card and wrap the text in a span:

```html
                  <svg ...></svg>
                  <span data-cms="pris_trener_p1">Ubegrenset antall kunder</span>
```
```html
                  <svg ...></svg>
                  <span data-cms="pris_trener_p2">Timeplan og booking</span>
```
```html
                  <svg ...></svg>
                  <span data-cms="pris_trener_p3">Fakturering og betaling</span>
```
```html
                  <svg ...></svg>
                  <span data-cms="pris_trener_p4">Sanntids chat</span>
```
```html
                  <svg ...></svg>
                  <span data-cms="pris_trener_p5">Integrasjoner (Fiken, Google Kalender)</span>
```

- [ ] **Step 14: Pris — customer feature list items**

Same pattern for the customer pricing card:
```html
                  <svg ...></svg>
                  <span data-cms="pris_kunde_p1">Book timer med din PT</span>
```
```html
                  <svg ...></svg>
                  <span data-cms="pris_kunde_p2">Chat direkte med treneren din</span>
```
```html
                  <svg ...></svg>
                  <span data-cms="pris_kunde_p3">Se din treningsplan og fremgang</span>
```
```html
                  <svg ...></svg>
                  <span data-cms="pris_kunde_p4">Betaling til treneren din</span>
```
```html
                  <svg ...></svg>
                  <span data-cms="pris_kunde_p5">Push-varsler og påminnelser</span>
```

- [ ] **Step 15: CTA section — heading and text (around line 1600–1606)**

Find:
```html
        <h2 class="text-3xl sm:text-5xl font-black tracking-[-0.02em] mb-4">
          Klar til å ta<br />
          <span class="gradient-text">PT-hverdagen din</span><br />
          til neste nivå?
        </h2>
        <p class="text-slate-400 text-lg leading-relaxed mb-10 max-w-xl mx-auto">
          Start din 30 dager gratis prøveperiode og opplev den enkleste måten å administrere PT-virksomheten din på.
        </p>
```
Replace with:
```html
        <h2 class="text-3xl sm:text-5xl font-black tracking-[-0.02em] mb-4">
          <span data-cms="cta_overskrift_1">Klar til å ta</span><br />
          <span class="gradient-text" data-cms="cta_overskrift_highlight">PT-hverdagen din</span><br />
          <span data-cms="cta_overskrift_3">til neste nivå?</span>
        </h2>
        <p class="text-slate-400 text-lg leading-relaxed mb-10 max-w-xl mx-auto" data-cms="cta_tekst">
          Start din 30 dager gratis prøveperiode og opplev den enkleste måten å administrere PT-virksomheten din på.
        </p>
```

- [ ] **Step 16: Load cms-inject.js before </body>**

Find the closing `</body>` tag and add the script just before it:
```html
  <script src="/cms-inject.js"></script>
</body>
```

- [ ] **Step 17: Verify page still looks correct**

Open `index.html` in a browser (or via `npx serve .`). The page should look identical to before — content.json values match the hardcoded HTML values, so nothing should visually change.

- [ ] **Step 18: Commit**

```bash
git add index.html
git commit -m "feat: add data-cms attributes and load cms-inject.js"
```

---

## Task 4: Create admin panel

**Files:**
- Create: `admin/index.html`
- Create: `admin/config.yml`

- [ ] **Step 1: Create admin/index.html**

```html
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex" />
    <title>Planra Admin</title>
  </head>
  <body>
    <script src="https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"></script>
  </body>
</html>
```

- [ ] **Step 2: Create admin/config.yml**

```yaml
backend:
  name: github
  repo: forsberg418/planra.website
  branch: main
  base_url: https://planra.no
  auth_endpoint: /api/auth

media_folder: uploads
public_folder: /uploads

collections:
  - name: innhold
    label: Nettside-innhold
    files:
      - name: settings
        label: Innhold
        file: content.json
        fields:

          # ── HERO ──────────────────────────────────────────────
          - label: "Badge-tekst (øverst i hero)"
            name: hero_badge
            widget: string

          - label: "Hero tittel – linje 1"
            name: hero_tittel_1
            widget: string

          - label: "Hero tittel – uthevet ord"
            name: hero_tittel_highlight
            widget: string

          - label: "Hero tittel – linje 3"
            name: hero_tittel_3
            widget: string

          - label: "Hero undertekst"
            name: hero_tekst
            widget: text

          # ── FOR TRENERE ───────────────────────────────────────
          - label: "Trener – overskrift"
            name: trener_overskrift
            widget: string

          - label: "Trener – uthevet del av overskrift"
            name: trener_overskrift_highlight
            widget: string

          - label: "Trener – brødtekst"
            name: trener_tekst
            widget: text

          - label: "Trener – app-bilde"
            name: trener_bilde
            widget: image

          - label: "Trener – funksjoner"
            name: trener_funksjoner
            widget: list
            fields:
              - { label: Tittel, name: tittel, widget: string }
              - { label: Beskrivelse, name: beskrivelse, widget: text }

          # ── FOR KUNDER ────────────────────────────────────────
          - label: "Kunde – overskrift"
            name: kunde_overskrift
            widget: string

          - label: "Kunde – uthevet del av overskrift"
            name: kunde_overskrift_highlight
            widget: string

          - label: "Kunde – brødtekst"
            name: kunde_tekst
            widget: text

          - label: "Kunde – app-bilde"
            name: kunde_bilde
            widget: image

          - label: "Kunde – funksjoner"
            name: kunde_funksjoner
            widget: list
            fields:
              - { label: Tittel, name: tittel, widget: string }
              - { label: Beskrivelse, name: beskrivelse, widget: text }

          # ── INNEBYGD CHAT ─────────────────────────────────────
          - label: "Chat – overskrift"
            name: chat_overskrift
            widget: string

          - label: "Chat – uthevet ord"
            name: chat_overskrift_highlight
            widget: string

          - label: "Chat – brødtekst"
            name: chat_tekst
            widget: text

          - label: "Chat – app-bilde"
            name: chat_bilde
            widget: image

          # ── PRIS ──────────────────────────────────────────────
          - label: "Trener-pris (kr/mnd)"
            name: pris_trener_kr
            widget: string

          - label: "Prøveperiode-tekst"
            name: pris_proveperiode
            widget: string

          - label: "Trener-plan funksjoner (5 stk)"
            name: pris_trener_punkter
            widget: list
            field: { label: Punkt, name: item, widget: string }

          - label: "Kunde-plan funksjoner (5 stk)"
            name: pris_kunde_punkter
            widget: list
            field: { label: Punkt, name: item, widget: string }

          # ── CTA ───────────────────────────────────────────────
          - label: "CTA overskrift – linje 1"
            name: cta_overskrift_1
            widget: string

          - label: "CTA overskrift – uthevet ord"
            name: cta_overskrift_highlight
            widget: string

          - label: "CTA overskrift – linje 3"
            name: cta_overskrift_3
            widget: string

          - label: "CTA undertekst"
            name: cta_tekst
            widget: text

          # ── FAQ ───────────────────────────────────────────────
          - label: "FAQ (spørsmål og svar)"
            name: faq
            widget: list
            fields:
              - { label: Spørsmål, name: sporsmal, widget: string }
              - { label: Svar, name: svar, widget: text }
```

- [ ] **Step 3: Commit**

```bash
git add admin/
git commit -m "feat: add Decap CMS admin panel and config"
```

---

## Task 5: Create GitHub OAuth Vercel functions

**Files:**
- Create: `api/auth.js`
- Create: `api/callback.js`

- [ ] **Step 1: Create api/auth.js**

```javascript
export default function handler(req, res) {
  const params = new URLSearchParams({
    client_id: process.env.OAUTH_CLIENT_ID,
    redirect_uri: 'https://planra.no/api/callback',
    scope: 'repo',
  });
  res.redirect(`https://github.com/login/oauth/authorize?${params}`);
}
```

- [ ] **Step 2: Create api/callback.js**

```javascript
export default async function handler(req, res) {
  const { code } = req.query;

  if (!code) {
    res.status(400).send('Missing code');
    return;
  }

  const tokenRes = await fetch('https://github.com/login/oauth/access_token', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      Accept: 'application/json',
    },
    body: JSON.stringify({
      client_id: process.env.OAUTH_CLIENT_ID,
      client_secret: process.env.OAUTH_CLIENT_SECRET,
      code,
    }),
  });

  const { access_token, error } = await tokenRes.json();

  if (error || !access_token) {
    res.status(401).send('OAuth failed: ' + (error || 'no token'));
    return;
  }

  const content = JSON.stringify({ token: access_token, provider: 'github' });

  res.send(`<!DOCTYPE html><html><body><script>
    (function() {
      window.opener.postMessage(
        'authorization:github:success:' + ${JSON.stringify(content)},
        '*'
      );
      window.close();
    })();
  </script></body></html>`);
}
```

- [ ] **Step 3: Commit**

```bash
git add api/
git commit -m "feat: add GitHub OAuth Vercel functions for Decap CMS"
```

---

## Task 6: Set up GitHub OAuth App + Vercel env vars (manual steps for Aleksander)

**No code changes — this is configuration.**

- [ ] **Step 1: Create GitHub OAuth App**

1. Go to: https://github.com/settings/developers → "OAuth Apps" → "New OAuth App"
2. Fill in:
   - **Application name:** `Planra CMS`
   - **Homepage URL:** `https://planra.no`
   - **Authorization callback URL:** `https://planra.no/api/callback`
3. Click "Register application"
4. On the next screen, copy the **Client ID**
5. Click "Generate a new client secret" and copy it immediately

- [ ] **Step 2: Add env vars to Vercel**

1. Go to Vercel → your planra.website project → Settings → Environment Variables
2. Add:
   - Name: `OAUTH_CLIENT_ID` — Value: (paste Client ID)
   - Name: `OAUTH_CLIENT_SECRET` — Value: (paste Client Secret)
3. Click Save, then **redeploy** the project (Vercel → Deployments → "..." → Redeploy)

- [ ] **Step 3: Add second user as GitHub collaborator**

1. Go to: https://github.com/forsberg418/planra.website/settings/access
2. Click "Add people" and invite the other user by their GitHub username
3. They must accept the invitation via email

---

## Task 7: Push and verify

- [ ] **Step 1: Push everything**

```bash
git push
```

- [ ] **Step 2: Verify the page still looks correct**

Open https://planra.no — the page should look identical to before.

- [ ] **Step 3: Verify admin panel loads**

Open https://planra.no/admin — you should see the Decap CMS login screen with "Login with GitHub".

- [ ] **Step 4: Log in and make a test edit**

1. Click "Login with GitHub" → authorize the app
2. Click "Nettside-innhold" → "Innhold"
3. Change the badge text to something temporary (e.g. add a "!" at the end)
4. Click "Publish" (top right)
5. Wait ~30 seconds, then check planra.no — the badge should show the updated text
6. Go back to admin and revert the change

---
